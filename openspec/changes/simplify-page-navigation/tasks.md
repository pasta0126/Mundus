## 1. Remove the navigation row

- [x] 1.1 Remove `PageNav` from the map, planet, system and dungeon pages and delete the component
- [x] 1.2 Confirm each page's "Mundus" title still links home and the dungeon page keeps "Back to map"

## 2. Verify and release

- [x] 2.1 Run the frontend type check, lint and build; check the four pages in the browser (no row, title leads home, dungeon returns to its map)
- [x] 2.2 Bump the semver (minor) and tag the commit `vX.Y.Z`
