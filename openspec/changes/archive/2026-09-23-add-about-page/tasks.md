## 1. Site metadata

- [x] 1.1 Add meta description and Open Graph/Twitter Card tags to `frontend/index.html` (reuse `mundus-icon-header.png` or a suitable existing image as the preview image).

## 2. About page

- [x] 2.1 Create `frontend/src/about/AboutPage.tsx` describing Mundus and its four features (map, planets, systems, dice), in English.
- [x] 2.2 Route `/about` to it in `frontend/src/main.tsx`.

## 3. Home hub footer

- [x] 3.1 Add a small footer to `frontend/src/home/HomePage.tsx` with a link to `/about`.

## 4. Spec sync

- [x] 4.1 Archive this change, syncing `about-page` (new) and `home-hub` (modified) into the main specs.
