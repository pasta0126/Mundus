## REMOVED Requirements

### Requirement: Touch-first devices are told the page is not optimized for them
**Reason**: The page is being made to work on phones and tablets (see `mobile-support`), so the notice that it does not is dropped.
**Migration**: None; nothing replaces it.
